{
  "name": "story-generation",
  "version": "1.0.0",
  "description": "Automated pipeline for generating cinematic survival scenes using AI models",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node src/main.js",
    "generate": "node src/main.js --production"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "openai": "^4.78.0",
    "@runwayml/sdk": "^3.15.0"
  },
  "devDependencies": {
    "vitest": "^3.0.4"
  }
}
